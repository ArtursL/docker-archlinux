A self-bootstrapping Arch Linux Docker container. The resulting image size is rather large (~700MB) so a flattened version (~300MB) is available as [splenetic/arch](https://hub.docker.com/u/splenetic/arch)
